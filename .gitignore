# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev.log
*.log

# env files (NEVER commit real secrets)
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

.vercel

# Producción: configurar las env vars en el panel de Vercel, no en el repo.
/.env.production

# Datos comerciales: la semilla de prospección lleva la lista de negocios con
# sus teléfonos, los mensajes y los precios. El repositorio es público, así que
# vive solo en local y en Supabase, no acá.
/scripts/seed-data-prospects.mjs
